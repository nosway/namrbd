# Public documentation build dependencies.
#
# Install with:
#   python -m pip install -r docs-src/requirements.txt
#
# Then build with `make docs-build` or serve locally with
# `mkdocs serve -f mkdocs.yml`.
#
# mkdocs-material pulls in mkdocs, pymdown-extensions, and the search plugin.
mkdocs-material>=9.7,<10
